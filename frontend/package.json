{
  "name": "gal2voice-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3015 --host 0.0.0.0 --strictPort",
    "build": "vite build",
    "deploy": "vite build && node deploy.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
